export enum Role {
  ADMIN = "admin",
  USER = "user",
}

export enum ProductStatus {
  AVAILABLE = "available",
  OUTOFSTOCK = "out-of-stock",
  DELETED = "deleted",
}

export enum ProductReturnType {
  FAULTY = "Faulty",
  DEFECTIVE = "Defective",
}

export enum ISort {
  ASC = "ASC",
  DESC = "DESC",
}

export enum IProductSearchKey {
  NAME = "name",
  STATUS = "status",
}
